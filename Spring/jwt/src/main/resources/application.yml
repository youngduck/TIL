server:
  port: 8084

spring:
  security:
    oauth2:
      client:
        registration:
          kakao:
            client-id: 027a7428e963b7efc2ef5dcab5dc30e2
            client-secret: CG3e7SEHBUvz1Sd2BRkR9XeY6UXVtpoJ
            redirect-uri: "http://localhost:3004/oauth2/callback/kakao"
            authorization-grant-type: authorization_code
            client-authentication-method: client_secret_post
            scope:
          naver:
            client-id: vYsDHVH3DFRiQC96G0d2
            client-secret: aBgJe5NKFL
            redirect-uri: "http://localhost:3004/oauth2/callback/naver"
            authorization-grant-type: authorization_code
            scope: email
        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: http://kapi.kakao.com/v2/user/me
            user-name-attribute: id
          naver:
            authorization-uri: https://nid.naver.com/oauth2.0/authorize
            token-uri: https://nid.naver.com/oauth2.0/token
            user-info-uri: http://openapi.naver.com/v1/nid/me
            user-name-attribute: response
  mvc:
    charset: UTF-8
  config:
    import: optional:file:.env
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${DB_HOST}:${DB_PORT}/${DB_NAME}?serverTimezone=Asia/Seoul&character_set_server=utf8mb4&collation-server=utf8mb4_general_ci&allowMultiQueries=true
    username: ${DB_USER}
    password: ${DB_PW}
  devtools:
    restart:
      enabled: true

mybatis:
  mapper-locations: mybatis/mapper/*.xml
  type-aliases-package: com.yd.jwt.dto

com:
  yd:
    jwt:
      secret-key: 2D9xQUVacO2D9xQUVacO2D9xQUVacO2D9xQUVacO2D9xQUVacO2D9xQUVacO2D9xQUVacO2D9xQUVacO

management:
  endpoints:
    web:
      base-path: /healthcheck
      path-mapping:
        health: healthcheck
  endpoint:
    health:
      show-details: always
  security:
    enable: false
