server:
  port: 8083

spring:
  security:
    oauth2:
      client:
        registration:
          kakao:
            client-id: 94de82d72c4f4a248930826808ac4b4f
            client-secret: 07jlow1ouZKzL3lW9u1Ikv0eWNmPgHWU
            redirect-uri: "http://localhost:8083/oauth2/callback/kakao"
            authorization-grant-type: authorization_code
            client-authentication-method: client_secret_post
            scope:
          naver:
            client-id: EX_Ff3cihpVCD3fxTITE
            client-secret: jn_oz0DcCn
            redirect-uri: "http://localhost:8083/oauth2/callback/naver"
            authorization-grant-type: authorization_code
            scope: email
        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: http://kapi.kakao.com/v2/user/me
            user-name-attribute: id
          naver:
            authorization-uri: https://nid.naver.com/oauth2.0/authorize
            token-uri: https://nid.naver.com/oauth2.0/token
            user-info-uri: http://openapi.naver.com/v1/nid/me
            user-name-attribute: response
  mvc:
    charset: UTF-8
  config:
    import: optional:file:.env
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${DB_HOST}:${DB_PORT}/${DB_NAME}?serverTimezone=Asia/Seoul&character_set_server=utf8mb4&collation-server=utf8mb4_general_ci&allowMultiQueries=true
    username: ${DB_USER}
    password: ${DB_PW}
  devtools:
    restart:
      enabled: true

mybatis:
  mapper-locations: mybatis/mapper/*.xml
  type-aliases-package: com.yd.jwt.dto

com:
  yd:
    jwt:
      secret-key: 2D9xQUVacO2D9xQUVacO2D9xQUVacO2D9xQUVacO2D9xQUVacO2D9xQUVacO2D9xQUVacO2D9xQUVacO

management:
  endpoints:
    web:
      base-path: /healthcheck
      path-mapping:
        health: healthcheck
  endpoint:
    health:
      show-details: always
  security:
    enable: false
